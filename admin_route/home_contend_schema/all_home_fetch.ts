import { Request, Response, NextFunction } from "express";
import dbconnection from "../../user_route--/DB/connection";

const allhome_cont = async (req: Request, res: Response) => {
  try {

   
   /// progrssing 
  } catch (error) {
    console.log(error);
  }
};
export default allhome_cont;
